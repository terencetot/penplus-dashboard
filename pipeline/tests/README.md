# Tests

All data in this suite is synthetic, built in-process to match the table and
heading structure `parse.py` expects (see `fixtures/build_synthetic_return.py`
and the `make_rec` factory in `conftest.py`). None of it is real WHO AFRO
country data, and none of it should be copied into `seed_history.py` or any
real return.
