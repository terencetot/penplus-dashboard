# Python
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/
.venv/
venv/

# Regional M&E source evidence -- not part of this repository
/data/raw/

# Demo bundle's SQLite intermediate -- regenerate with
# pipeline/tools/generate_demo_bundle.py; the JSON it writes to
# site/public/demo-data IS committed, since the deployed site serves it.
pipeline/src/penplus_pipeline/demo.db

# consolidate.py's outputs: an internal-view partner workbook and per-country
# data-quality reports, regenerated on demand (`make reports`) and shared
# directly, not published -- this repo is public, and a report can name a
# specific country's data-quality gap the way the public bundle never does.
/reports/

# Node / frontend
site/node_modules/
site/dist/
site/.vite/
*.log
npm-debug.log*

# Editors / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# Build artifacts
*.tmp
